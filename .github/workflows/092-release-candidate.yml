name: TuTop 0.9.2 candidate workflow (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, linux, x64, tutop-zero-cost-controller]
    steps:
      - name: Explain replacement
        run: |
          echo "This workflow was retired because it checked package.json with node before setup-node ran."
          echo "Use .github/workflows/092-release-candidate-v2.yml instead."
